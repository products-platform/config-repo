server:
  port: 8000

spring:
  application:
    name: order-service

  datasource:
    url: jdbc:mysql://localhost/order_dev_db
    username: dev_user
    password: dev_pass

logging:
  level:
    root: DEBUG

custom:
  message: "ORDER-SERVICE - DEV ENVIRONMENT"
