server:
  port: 8003

spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:mysql://localhost/notification_dev_db
    username: dev_user
    password: dev_pass

logging:
  level:
    root: DEBUG

custom:
  message: "NOTIFICATION-SERVICE - DEV ENVIRONMENT"
