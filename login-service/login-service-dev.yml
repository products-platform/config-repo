server:
  port: 8012

spring:
  application:
    name: login-service

  datasource:
    url: jdbc:mysql://localhost/login_dev_db
    username: dev_user
    password: dev_pass

logging:
  level:
    root: DEBUG

custom:
  message: "LOGIN-SERVICE - DEV ENVIRONMENT"
