server:
  port: 8006

spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://localhost/payment_dev_db
    username: dev_user
    password: dev_pass

logging:
  level:
    root: DEBUG

custom:
  message: "PAYMENT-SERVICE - DEV ENVIRONMENT"
