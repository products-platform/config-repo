server:
  port: 8009

spring:
  application:
    name: product-service

  datasource:
    url: jdbc:mysql://localhost/product_dev_db
    username: dev_user
    password: dev_pass

logging:
  level:
    root: DEBUG

custom:
  message: "PRODUCT-SERVICE - DEV ENVIRONMENT"
